const QuoteSection = () => {
    return <section style={{height: "calc(100vh - 64px)"}} className="flex flex-col justify-center items-center text-foreground bg-background">
        Quote Section
    </section>
}

export default QuoteSection